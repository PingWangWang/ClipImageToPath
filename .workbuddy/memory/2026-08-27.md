# 2026-08-27

## 新增「图片转路径」功能总开关（托盘菜单）
- 需求：系统托盘右键菜单增加开关，控制核心功能（剪贴板图片→路径）是否启用。默认开启，状态持久化。
- 方案：服务层软门控（复用「消息提示」CheckOnClick + 注册表模式），改动最小、风险最低。
- 变更文件：
  - `src/ImageToPathService.cs`：新增 `Enabled` 属性与 `SetEnabled(bool)`（关闭时取消挂起回写+清理临时文件）；`OnClipboardUpdated` 起始 `if(!Enabled) return;`。
  - `src/TrayApplicationContext.cs`：构造注入 `ImageToPathService`；新增菜单项「图片转路径」（CheckOnClick）、`IsFeatureEnabled()`、`ToggleFeature()`（写注册表 `FeatureEnabled` + 通知服务 + 更新托盘 Text）；菜单项置于顶部。
  - `src/Program.cs`：构造传参；`--selftest` 强制 `service.SetEnabled(true)`。
  - `README.md`：补充菜单项说明。
- 注册表键：`Software\ClipImageToPath` 值 `FeatureEnabled`（DWord，缺省 1）。
- 验证：`dotnet build -c Debug` 通过（0 警告 0 错误）。未在本环境实跑 exe（无桌面会话）。

## 版本号更新
- 用户确认将版本升到 1.0.5（补丁级）。
- 改动：`src/ClipImageToPath.csproj` `<Version>` 1.0.4 → 1.0.5（版本唯一来源）；同步 README 与 `scripts/publish.ps1` 注释中的版本示例（原 1.0.0）。
- `build/obj/` 下生成的 `AssemblyInfo.cs` 仍为旧版本，下次构建自动刷新，无需手动改。
